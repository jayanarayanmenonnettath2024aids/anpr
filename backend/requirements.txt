# ANPR Backend Dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==12.0
aiosqlite==0.20.0
opencv-python-headless==4.10.0.84
numpy>=1.26.0
pydantic>=2.0.0
python-multipart==0.0.9

# Primary OCR stack:
paddleocr>=2.7.0

# PaddlePaddle is the backend for PaddleOCR. Keep this pinned to a version
# that matches the local Python/runtime constraints.
paddlepaddle>=2.6.0

# Optional detector backend for the INPR model:
# detectron2

# Fallback (using this due to Python version):
pytesseract>=0.3.10
